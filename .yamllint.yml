# contracts/*.yml use deliberate column-aligned flow style (extra spaces for
# readability) — relax the rules that would otherwise flag that on purpose.
extends: default

rules:
  document-start: disable
  braces:
    max-spaces-inside: -1
  commas:
    max-spaces-after: -1
  line-length:
    max: 180
